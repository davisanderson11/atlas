# Reduce package installation size
omit=dev
package-lock=false
save-exact=true

# Skip downloading Electron if already cached
electron_skip_binary_download=1

# Reduce native module rebuild verbosity
npm_config_loglevel=error